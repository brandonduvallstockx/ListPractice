import React from 'react';
import NumberListContainer from '../container/NumberListContainer';
import RefreshButton from './RefreshButton';

const App = () => (
  <div>
  <NumberListContainer />
  <RefreshButton />
  </div>
);

export default App;
